You are a senior quantitative developer working on an existing .NET backtesting engine.

I already have a method called RunORB that implements a basic Opening Range Breakout strategy.

Your task is to IMPROVE this method without changing its structure drastically.

---------------------------------------

GOAL

Upgrade the ORB strategy from a naive breakout system to a more realistic, professional-grade intraday strategy.

Do NOT rewrite everything.

Enhance logic inside existing flow.

---------------------------------------

CURRENT LOGIC ISSUES

- Direct breakout entries (no confirmation)
- No trend filter
- Weak volume validation
- Too many false breakouts
- No volatility filtering beyond ATR
- No retest logic

---------------------------------------

REQUIRED IMPROVEMENTS

1) BREAKOUT VALIDATION

- Only consider CLOSE breakout (already present)
- Add buffer:
    breakout must exceed openingHigh/Low by at least 0.05%

---------------------------------------

2) RETEST ENTRY (IMPORTANT)

Do NOT enter immediately on breakout.

Instead:

- Wait for price to RETEST breakout level
- Enter only if price holds above (bullish) or below (bearish)

---------------------------------------

3) VOLUME CONFIRMATION

Enhance existing volume logic:

- Breakout candle volume must be > 1.5x average of last 10 candles

---------------------------------------

4) TREND FILTER (15-min logic approximation)

Add lightweight trend filter using existing indicators:

- Use EMA(20)

Rules:

Bullish:
    price > EMA20
    EMA slope upward

Bearish:
    price < EMA20
    EMA slope downward

---------------------------------------

5) VOLATILITY FILTER

Skip day if:

- Opening range < 0.3% of price (low volatility)
- Opening range > 2.5% (extreme volatility)

---------------------------------------

6) TIME FILTER

Allow trades only:

- 09:20 to 10:30 IST

Stop new entries after cutoff.

---------------------------------------

7) FALSE BREAKOUT PROTECTION

Ignore breakout if:

- Next candle immediately reverses fully inside range

---------------------------------------

8) RISK MANAGEMENT

Keep existing logic but ensure:

- Minimum RR = 2.5
- Prefer 3:1

---------------------------------------

9) TRADE LIMIT

- Max 1–2 trades per day

---------------------------------------

10) CLEAN CODE

- Do NOT break method signature
- Add helper methods where needed
- Keep it readable

---------------------------------------

OUTPUT

Return improved RunORB method with:

- Comments explaining each improvement
- Minimal structural disruption
- Production-quality logic

---------------------------------------

GOAL

Reduce overtrading and false signals.

Focus on high-quality trades.











You are a senior .NET quantitative developer.

I have an existing backtesting engine with a method RunORB.

Your task is to create a NEW strategy method:

RunOrbFvgStrategy

This should integrate cleanly into the existing system.

---------------------------------------

GOAL

Implement a high-quality intraday strategy based on:

- Opening Range Breakout (ORB)
- Fair Value Gap (FVG)
- Retracement
- Engulfing confirmation

This is a multi-step strategy.

---------------------------------------

STRATEGY LOGIC

STEP 1: OPENING RANGE

- Use first 5 candles (1-min) → 09:15–09:20
- Define:
    openingHigh
    openingLow

---------------------------------------

STEP 2: BREAKOUT DETECTION

After 09:20:

Bullish:
    close > openingHigh

Bearish:
    close < openingLow

IMPORTANT:

- Only CLOSE breakout
- Ignore wicks

---------------------------------------

STEP 3: STATE MACHINE (VERY IMPORTANT)

Implement a state-based flow:

States:

- WaitingForBreakout
- WaitingForFvg
- WaitingForRetrace
- WaitingForEngulfing
- TradeTriggered

---------------------------------------

STEP 4: FVG DETECTION

Use 3-candle structure:

Bullish:
    candle1.high < candle3.low

Bearish:
    candle1.low > candle3.high

Rules:

- Must form AFTER breakout
- Ignore small gaps:
    minimum gap = 0.1% price

---------------------------------------

STEP 5: RETRACE INTO FVG

- Wait for price to come back into FVG zone
- No chasing breakout

---------------------------------------

STEP 6: ENGULFING CONFIRMATION

Bullish:

- Current candle engulfs previous
- Close > previous high

Bearish:

- Current candle engulfs previous
- Close < previous low

---------------------------------------

STEP 7: TREND FILTER

Use EMA(20):

Bullish:
    price > EMA
    EMA rising

Bearish:
    price < EMA
    EMA falling

---------------------------------------

STEP 8: VOLUME FILTER

- Engulfing candle volume > average last 10 candles

---------------------------------------

STEP 9: ENTRY

- Entry at next candle OPEN

---------------------------------------

STEP 10: STOP LOSS

Bullish:
    below FVG low

Bearish:
    above FVG high

---------------------------------------

STEP 11: TARGET

- Risk:Reward = 3:1

---------------------------------------

STEP 12: TIME FILTER

- Trade only between 09:20–10:30 IST

---------------------------------------

STEP 13: RISK CONTROL

- Max 1 trade per day
- Skip low volatility days

---------------------------------------

STEP 14: OUTPUT

Return List<BacktestTradeResult>

Use existing:

- cost functions
- slippage
- quantity logic

---------------------------------------

STEP 15: CODE REQUIREMENTS

- Clean C#
- Reuse existing utilities
- Add helper methods:

    DetectFvg()
    IsEngulfing()
    IsTrendValid()

- Use clear variable names
- Comment each step

---------------------------------------

GOAL

This strategy should produce:

- Fewer trades
- Higher accuracy
- Better RR trades

Avoid overtrading.

Focus on precision.


Cannot write DateTimeOffset with Offset=05:30:00 
to PostgreSQL type 'timestamp with time zone'
only offset 0 (UTC) is supported


------------------------------------------------------------######################################

You are a senior software architect and quantitative trading engineer.

I already have an existing .NET system with:

* APIs to fetch market data (Nifty/options)
* A working backtesting engine
* A UI to visualize results

Your task is to DESIGN and IMPLEMENT a modular “Strategy Research Engine” that builds on top of my existing system.

Do NOT rewrite existing functionality. Instead:

* Discover and reuse existing services
* Extend them cleanly using interfaces and dependency injection
* Keep everything production-grade and maintainable

---

## 🎯 Goal

Create a system that can:

1. Generate trading strategies
2. Run backtests using my existing engine
3. Evaluate performance
4. Improve strategies iteratively
5. Simulate a multi-agent discussion in a structured way

---

## 🧩 Architecture Requirements

Design the system with the following components:

### 1. Orchestrator (Core Engine)

* Controls the full loop:
  Generate → Backtest → Evaluate → Improve → Repeat
* Configurable number of iterations
* Logs every step

---

### 2. Strategy Model

Define a flexible strategy schema (class or JSON) with:

* Entry conditions (e.g., breakout, delta threshold)
* Exit rules (SL, target)
* Filters (volume, trend, theta constraints)

Ensure it is extensible.

---

### 3. Strategy Generator

* Generates initial strategies (rule-based first, then AI-ready)
* Should support:

  * Randomized parameter generation
  * Template-based strategies

---

### 4. Backtest Adapter

* Connects to my existing backtest system
* Accepts Strategy Model
* Returns:

  * PnL
  * Win rate
  * Drawdown
  * Trade logs

---

### 5. Evaluation Engine

* Scores strategies based on:

  * Profitability
  * Drawdown
  * Risk-reward
* Produces a normalized score

---

### 6. Improvement Engine (Agent-like behavior)

* Takes a weak strategy and improves it by:

  * Adjusting parameters (delta, theta thresholds)
  * Adding/removing conditions
* Should NOT be random — must use evaluation feedback

---

### 7. Critic / Validator

* Rejects:

  * Overfitted strategies
  * Unrealistic assumptions
* Adds constraints like:

  * Minimum trades
  * Max drawdown limits

---

### 8. Strategy Memory

* Store all strategies tested
* Avoid duplicates
* Track best-performing strategies

---

## 🤖 “Agent Discussion” Simulation

Do NOT implement free-form chat agents.

Instead simulate agents as structured roles:

* Generator → proposes strategy
* Evaluator → scores it
* Critic → challenges it
* Improver → modifies it

Each step must pass structured data (not plain text).

---

## ⚙️ Technical Requirements

* Use clean architecture (interfaces, services)
* Use dependency injection
* Keep components loosely coupled
* Add logging at every stage
* Make it easy to plug in LLM later

---

## 📦 Deliverables

1. Folder structure for new modules
2. Interfaces and implementations for all components
3. Example workflow execution
4. Sample strategies and outputs
5. Extension points for AI integration later

---

## 🚨 Constraints

* DO NOT break existing APIs
* DO NOT hardcode values
* DO NOT tightly couple components
* Ensure testability

---

## 💡 Bonus (if possible)

* Add parallel strategy testing
* Add configuration for risk parameters
* Add ability to export best strategies

---

Start by:

1. Analyzing what components likely already exist in my system
2. Designing the architecture
3. Then implementing step-by-step

Output clean, production-quality .NET code.



------------------------------------------------------------######################################
I have an existing stock backtesting platform. Before making any changes,
analyze the entire codebase thoroughly and understand:

1. How the existing backend is structured (controllers, services, models)
2. How the existing strategy/strategies are currently implemented
3. What the backtest API currently returns (response shape, fields)
4. How the frontend config page works — how it reads the selected strategy
   from the dropdown and renders config fields dynamically
5. What the frontend does with the backtest API response (charts, tables,
   stats displayed)

---

TASK 1 — BACKEND REFACTORING (do this first):

The current strategy logic is all in a single class. Refactor it into a
clean, extensible strategy architecture:

- Create an IStrategy interface with a method like:
  BacktestResult Run(List<Candle> candles, StrategyConfig config)
- Move the existing strategy into its own class implementing IStrategy
- Create a StrategyRegistry or factory that maps strategy name (string)
  to its IStrategy implementation
- This pattern must make it trivial to add new strategies by just adding
  a new class — no changes to the controller or engine

---

TASK 2 — ADD EMA CROSSOVER STRATEGY:

Create a new class EmaCrossoverStrategy : IStrategy

This strategy must support ALL of the following config parameters
(expose each as a configurable field, not hardcoded):

CORE EMA SETTINGS:
- FastEma (int)
- SlowEma (int)
- MiddleEma (int) — only used when UseTripleEma = true

TIMEFRAME (drives frontend defaults only, not backend logic):
- Timeframe: enum { Intraday, Swing, Both }

CONFIRMATION FILTER — user picks ONE of these four modes:
- FilterType: enum { RSI, Volume, SupportResistance, PriceAction }

RSI filter config (active when FilterType = RSI):
- RsiPeriod (int, default 14)
- RsiMidline (int, default 50)
- RsiOverbought (int, default 70)
- RsiOversold (int, default 30)

Volume filter config (active when FilterType = Volume):
- VolumeAvgPeriod (int, default 20)
- VolumeMultiplier (decimal, default 1.5)

Support & Resistance filter config (active when FilterType = SupportResistance):
- SRLookbackPeriod (int, default 20)
- SRBuffer (decimal, default 0.5)

Price Action filter config (active when FilterType = PriceAction):
- AllowedPatterns: multi-select from [ Engulfing, Hammer, Doji, MorningStar ]
- CandleLookback (int, default 1)

TRIPLE EMA MODE:
- UseTripleEma (bool, default false)
- When enabled: entry only when FastEma > MiddleEma > SlowEma (long)
  or FastEma < MiddleEma < SlowEma (short)

TRADE MANAGEMENT:
- StopLossType: enum { FixedPercent, BelowEma, AtrBased }
- StopLossValue (decimal)
- AtrPeriod (int, default 14) — used when StopLossType = AtrBased
- TargetRRR (decimal, default 2.0)
- MaxHoldingPeriods (int)

DIRECTION:
- TradeDirection: enum { LongOnly, ShortOnly, Both }

---

STRATEGY LOGIC (implement exactly as described):

LONG ENTRY:
1. FastEma crosses above SlowEma on current candle
2. If UseTripleEma: FastEma > MiddleEma > SlowEma must all be stacked
3. Apply selected FilterType check:
   - RSI: RSI > RsiMidline AND RSI < RsiOverbought
   - Volume: current candle volume > (VolumeAvgPeriod average × VolumeMultiplier)
   - SupportResistance: price is within SRBuffer% of a identified support
     level from the last SRLookbackPeriod candles
   - PriceAction: one of AllowedPatterns detected within CandleLookback
     candles before entry
4. All conditions pass → enter Long on next candle open

SHORT ENTRY:
1. FastEma crosses below SlowEma
2. If UseTripleEma: FastEma < MiddleEma < SlowEma stacked bearishly
3. Apply selected FilterType check (inverted):
   - RSI: RSI < RsiMidline AND RSI > RsiOversold
   - Volume: same volume condition (volume spike confirms move)
   - SupportResistance: price is within SRBuffer% of identified resistance
   - PriceAction: bearish pattern detected within CandleLookback candles
4. All conditions pass → enter Short on next candle open

EXIT:
- SL hit → exit immediately on that candle
- Target hit (entry ± StopLoss × TargetRRR) → exit
- MaxHoldingPeriods exceeded → exit at close of that candle
- Opposite crossover signal → exit current trade

STOP LOSS CALCULATION:
- FixedPercent: SL = entry price ± StopLossValue%
- BelowEma: SL = SlowEma value at entry candle (long: below, short: above)
- AtrBased: SL = entry price ± (ATR(AtrPeriod) × StopLossValue)

---

TASK 3 — FRONTEND CONFIG UI:

Analyze how the existing frontend config page renders fields for other
strategies. Follow the exact same pattern for EMA Crossover.

When "EMA Crossover" is selected from the strategy dropdown, render
the config fields in this order:

STEP 1 — TIMEFRAME SELECTOR (render this first):
- Show three options: Intraday | Swing | Both
- When user selects Intraday → auto-fill:
    FastEma = 9, SlowEma = 21, MiddleEma = 21
- When user selects Swing → auto-fill:
    FastEma = 20, SlowEma = 50, MiddleEma = 21
- When user selects Both → no auto-fill, leave fields blank or at last
  user-set value
- All EMA fields remain fully editable after auto-fill — these are
  smart defaults, not locked values
- If the user manually changes an EMA value after auto-fill, do not
  reset it when they switch timeframe unless they switch timeframe again

STEP 2 — EMA FIELDS:
- FastEma (number input)
- SlowEma (number input)
- UseTripleEma (toggle) — when enabled, show MiddleEma (number input)
  immediately below

STEP 3 — FILTER TYPE:
- FilterType dropdown (RSI / Volume / Support & Resistance / Price Action)
- Conditionally show ONLY the relevant filter config fields below based
  on selection — hide all others completely (not just disabled)

STEP 4 — TRADE MANAGEMENT:
- StopLossType dropdown
- StopLossValue input
- AtrPeriod input — show only when StopLossType = AtrBased
- TargetRRR input
- MaxHoldingPeriods input
- TradeDirection dropdown

All fields must have sensible defaults pre-filled as specified above.
The config object sent to the API must match exactly what the backend
StrategyConfig model expects — analyze the existing API contract and
follow the same request shape.

---

TASK 4 — API CONTRACT:

Analyze the existing backtest API response shape. Make the EMA Crossover
strategy return results in the exact same shape so the existing frontend
charts, tables, and stats work without any changes.

---

IMPORTANT INSTRUCTIONS FOR COPILOT:
- Analyze first, code second. Do not assume anything about the codebase.
- Follow the existing code style, naming conventions, and folder structure
  exactly.
- Do not break or modify any existing strategy behavior.
- The refactoring in Task 1 must be fully backward compatible — existing
  API endpoints and frontend behavior must not change.
- If anything is ambiguous after analyzing the codebase, ask before
  generating code.



###################################################################################

API prompt
Build the radar backend and response shaping for the /api/radar/sections endpoint.

Requirements:

Optimize RadarController.GetSections to avoid N+1 candle queries.
Batch-load candles for all instruments once per request, then reuse for:
GetMoversAsync
GetBreakoutsAsync
GetPatternsAsync
Keep GetNearSRAsync using indicator snapshots.
Add performance timing/logging for each section.
Extend radar DTOs so top gainers and losers include:
TrendCandles (last 5 days of candles or last 20 intervals)
AIAnalysis placeholder text
Ensure existing ScanResult/snapshot data still supports symbol, exchange, score, bias, and last close.
Files to update:

RadarController.cs
src/TradingSystem.Api/DTOs/RadarDTOs.cs
MarketScannerService.cs
TradingSystem.MarketData service layer for batched candle queries
UI prompt
Build the radar UI view for top gainers and losers with mini-trend candles.

Requirements:

Show a Top Gainers and Top Losers section.
For each stock, display:
symbol, exchange
current price / last close
change %
and setup score/bias
a small 5-day trend sparkline or mini-candlestick row
Use the new TrendCandles data from the API.
Reserve space for future AI explanations like:
“Why this stock is rising”
“Why this stock is falling”
Make the trend widget compact and easy to scan.
Suggested UI approach:

TrendCandles → render 5 small candlesticks row
add hover tooltip with daily open/high/low/close
label each row with score, bias, and direction
Combined task prompt
Build the radar endpoint and UI together, focusing on speed and rich trend context.

API: optimize GetSections, batch candle retrieval, enrich top gainers/losers with trend candle data and AI analysis placeholders.
UI: render top gainers/losers with compact 5-day mini-candles, score, bias, change %, and future AI reason section.
Use this as the prompt in your other tab.


#########################################################

Before writing any code, read and understand the entire solution structure:
- Identify all project names and their roles (API, Domain, Infrastructure or equivalent)
- Find all existing services that deal with: OHLC/candle data, technical 
  indicators, backtesting, stock metadata, and any market data fetching
- Note the exact interface names, implementation class names, method signatures, 
  and return types of these services
- Find how DI is registered (look at Program.cs and any extension methods like 
  AddInfrastructure(), AddApplication() etc.)
- Find the EF Core DbContext and IDbContextFactory usage pattern
- Find the Quartz.NET job registrations so you don't touch them
- Find the existing CORS configuration if any
- On the React side: find the component that renders the stock chart, how it 
  receives symbol and timeframe (props vs route params), the existing API 
  service layer structure, env variable naming convention, and Tailwind theme

Only after fully reading the codebase, proceed with the following tasks:

---

TASK 1 — Add MCP Server to the .NET API

The goal is to expose stock data from the existing database as MCP tools 
so Claude AI can call them on demand to analyze stocks. MCP (Model Context 
Protocol) is a standard protocol — install the official SDK:

  In the API project (.csproj that contains Program.cs):
  dotnet add package ModelContextProtocol.AspNetCore

Create a new folder called McpTools inside the API project.
Create a file McpTools/StockAnalysisTools.cs inside the API project.

Rules for this file:
- Mark the class with [McpServerToolType] attribute
- Inject ONLY the service interfaces that already exist in the codebase 
  (the ones you found above) — do not create any new services or repos
- If a service for a specific tool doesn't exist, skip that tool and add 
  a comment saying which service is missing
- Every tool method must be: public async Task<string> with [McpServerTool] 
  and [Description("...")] attributes
- Every parameter must have a [Description("...")] attribute
- Return type is always JSON string (use System.Text.Json.JsonSerializer)
- Respect the IDbContextFactory pattern if the services use it

Define the following MCP tools — but only if the underlying data exists:

Tool 1: GetOhlcData
  Parameters: symbol (NSE/BSE ticker), timeframe (daily/weekly/15min etc.), 
  candleCount (int, how many recent candles)
  Description: "Fetch recent OHLC candle data for a stock from the database"
  Action: call the relevant existing service method, return candles as JSON

Tool 2: GetIndicatorSnapshot  
  Parameters: symbol, timeframe
  Description: "Get latest indicator values for a stock: EMA, RSI, MACD, 
  ADX, ATR, Bollinger Bands — whatever indicators are stored in the DB"
  Action: call the relevant existing service, return all indicator values as JSON

Tool 3: GetSupportResistanceLevels
  Parameters: symbol, timeframe
  Description: "Get key support and resistance price levels for a stock"
  Action: if a dedicated service exists use it; otherwise derive from 
  recent swing highs/lows in OHLC data using the candle service

Tool 4: GetBacktestSummary
  Parameters: symbol, strategyName
  Description: "Get backtest result summary for a stock — win rate, avg 
  profit/loss, max drawdown, total trades"
  Action: call the backtest service if it exists; skip if not found

Tool 5: GetStockSummary
  Parameters: symbol
  Description: "Get a high-level summary of a stock: latest price, day 
  range, 52-week high/low, and any metadata available"
  Action: aggregate from whatever services have this data

Register MCP in Program.cs:
- Add this in the service registration section (before app.Build()), 
  after all existing service registrations:
    builder.Services.AddMcpServer()
        .WithHttpTransport()
        .WithTools<StockAnalysisTools>();
- Add this in the middleware pipeline (before app.Run()):
    app.MapMcp("/mcp");
- Do NOT modify or remove any existing registrations, CORS policies, 
  Quartz jobs, or controller mappings
- If AddInfrastructure() or similar extension methods register services, 
  place AddMcpServer() after them

If a CORS policy already exists, extend it to also allow the /mcp path 
and localhost:3000 and localhost:5173 origins. Do not create a duplicate 
CORS policy.

Create a file at the solution root: claude_desktop_config.json
Content:
{
  "mcpServers": {
    "stock-analyzer": {
      "url": "http://localhost:<YOUR_API_PORT>/mcp"
    }
  }
}
Replace <YOUR_API_PORT> with the actual port from launchSettings.json.
Add claude_desktop_config.json to .gitignore.
Add this section to README.md:

## Claude Desktop MCP Setup
To connect Claude Desktop to the local stock analysis MCP server:
1. Run the API locally
2. Copy claude_desktop_config.json to:
   - Windows: %APPDATA%\Claude\claude_desktop_config.json
   - macOS: ~/Library/Application Support/Claude/claude_desktop_config.json
3. Restart Claude Desktop
4. Ask Claude: "Analyze RELIANCE on the daily timeframe"

---

TASK 2 — Add Stock Analysis Panel to the React UI

The goal is a panel on the existing stock chart page where the user clicks 
"Analyze with AI", the app calls Claude API with the stock's data as context, 
and Claude returns a structured analysis rendered in the UI.

Read the React codebase first:
- Find the chart page component and how symbol + timeframe are available in it
- Find the existing API service layer (how fetch calls are structured, 
  base URL, headers pattern)
- Find the env variable naming convention (VITE_ prefix assumed, confirm)
- Find the Tailwind color scheme and dark/light mode pattern

Create src/services/claudeService.ts:
- Export: async function analyzeStock(symbol: string, timeframe: string): Promise<string>
- Inside:
  a. Use the EXISTING API service functions to fetch:
       - Indicator snapshot for the symbol + timeframe
       - Last 10 OHLC candles for the symbol + timeframe  
       - Stock summary/metadata for the symbol
     Do not call the .NET API directly with raw fetch here — use whatever 
     service abstraction already exists in the codebase
  b. Build a prompt string using this exact template:
     """
     You are an expert stock analyst for Indian equity markets (NSE/BSE).
     Analyze the following data and give a structured analysis.

     Stock: {symbol}
     Timeframe: {timeframe}

     --- Technical Indicators ---
     {indicatorSnapshot as formatted JSON}

     --- Recent OHLC Candles (last 10) ---
     {ohlcData as formatted JSON}

     --- Stock Summary ---
     {stockSummary as formatted JSON}

     Provide the following sections:
     1. Trend Analysis (short-term and medium-term)
     2. Momentum Assessment (RSI, MACD, ADX interpretation)
     3. Key Support and Resistance levels
     4. Volume Observations
     5. Risk Factors to watch
     6. Overall Bias: Bullish / Bearish / Neutral — with clear reasoning
     """
  c. POST to https://api.anthropic.com/v1/messages with:
       headers:
         "Content-Type": "application/json"
         "x-api-key": import.meta.env.VITE_ANTHROPIC_API_KEY
         "anthropic-version": "2023-06-01"
       body:
         model: "claude-opus-4-5"
         max_tokens: 2000
         messages: [{ role: "user", content: <the prompt> }]
  d. Return data.content[0].text
  e. On any error, throw a descriptive Error with the status code

Create src/components/StockAnalysisPanel.tsx:
- Props: { symbol: string; timeframe: string }
- Internal state: loading (boolean), result (string | null), error (string | null)
- Render:
  - A button "Analyze with AI" — onClick calls analyzeStock and sets state
  - While loading: show a spinner or "Analyzing..." text
  - On result: render the analysis text in a styled div. The text will 
    contain numbered sections — render it as preformatted or with 
    whitespace-pre-wrap so line breaks are preserved
  - On error: show the error message in red
- Styling: use Tailwind, match the existing dark card style of the UI
- Do NOT use any <form> tags — only onClick handlers
- Do NOT install any new packages

Integrate StockAnalysisPanel into the existing chart page:
- Find the chart page component
- Import StockAnalysisPanel
- Place <StockAnalysisPanel symbol={symbol} timeframe={timeframe} /> 
  directly below the chart — pass the same symbol and timeframe the 
  chart is already using
- Make zero other changes to the chart page

Add to .env.local (create if it doesn't exist):
  VITE_ANTHROPIC_API_KEY=your_key_here
Add .env.local to .gitignore if not already there.

---

FINAL CHECKLIST before finishing:
- StockAnalysisTools.cs only uses service interfaces that already exist 
  in the codebase — no new repos or DB calls directly
- Program.cs has no duplicate registrations and Quartz jobs are untouched
- MapMcp("/mcp") is present and does not conflict with existing routes
- claudeService.ts uses existing API service abstractions, not raw fetch 
  to the .NET API
- StockAnalysisPanel.tsx has no <form> tags
- .env.local and claude_desktop_config.json are both in .gitignore
- README.md has the Claude Desktop setup instructions

List every file you created or modified, and for modified files, 
show exactly which lines were added.
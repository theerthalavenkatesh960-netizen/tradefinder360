
CONTEXT:
I have a MarketSentimentService.cs in a C# .NET trading system. The service currently calculates market sentiment using only today's candles, which is fundamentally wrong. I need it rebuilt with proper multi-period historical analysis.

CRITICAL BUG TO FIX FIRST:
The daily % change calculation is wrong everywhere in the file:
csharp// WRONG (current code)
var changePercent = (latestCandle.Close - firstCandle.Open) / firstCandle.Open * 100m;

// CORRECT — always use previous day's close as the base
var changePercent = (todayClose - previousDayClose) / previousDayClose * 100m;
Fix this in AnalyzeIndicesAsync, AnalyzeSectorsAsync, and CalculateMarketBreadthAsync.

DATA FETCHING CHANGES:
The repository method signature is:
csharpTask<List<MarketCandle>> GetByInstrumentIdAsync(
    Guid instrumentId, int intervalMinutes, DateTime from, DateTime to, CancellationToken ct);
```

Fetch the following windows (all dates in IST, pass `utcNow` as the `to` parameter):

| Purpose | From | Interval |
|---|---|---|
| Daily change (need prev close) | `istToday.AddDays(-3)` | 1 day (1440 min) |
| RSI-14 | `istToday.AddDays(-21)` | 1 day |
| MACD (26 EMA + 9 signal) | `istToday.AddDays(-60)` | 1 day |
| 20DMA / breadth trend | `istToday.AddDays(-30)` | 1 day |
| 50DMA | `istToday.AddDays(-75)` | 1 day |
| VIX trend (20-day avg) | `istToday.AddDays(-30)` | 1 day |
| 52-week high/low | `istToday.AddDays(-365)` | 1 day |

Fetch the **widest window needed per instrument once** and slice it for each indicator — do not make multiple repository calls per instrument.

---

**INDICATORS TO COMPUTE:**

**1. RSI-14** (per index, per sector avg, and market-wide)
```
RS = AvgGain(14) / AvgLoss(14)
RSI = 100 - (100 / (1 + RS))
```
- Use Wilder's smoothing (not simple average)
- RSI > 70 → overbought, RSI < 30 → oversold

**2. MACD** (per index)
```
MACD Line   = EMA(12) - EMA(26)
Signal Line = EMA(9) of MACD Line
Histogram   = MACD Line - Signal Line
```
- Histogram > 0 and rising → bullish momentum
- Histogram < 0 and falling → bearish momentum

**3. Moving Average Trend**
```
Price vs 20DMA → short-term trend
Price vs 50DMA → medium-term trend
20DMA vs 50DMA → golden cross / death cross
```

**4. Market Breadth (rolling, not single-day)**
- Compute A/D ratio as a **20-day rolling average**, not just today
- Add 52-week new highs count vs new lows count across the stock universe
- McClellan Oscillator = EMA(19) of daily A/D diff - EMA(39) of daily A/D diff

**5. VIX Trend**
```
VIX vs its own 20-day SMA:
  VIX > 20DMA and rising → fear increasing → bearish signal
  VIX < 20DMA and falling → complacency → neutral/bullish

REVISED SENTIMENT SCORE (total = 100 pts):
ComponentWeightSignalIndex trend (MA + price)25 ptsPrice vs 20DMA/50DMA, golden/death crossIndex momentum (RSI + MACD)20 ptsRSI zone + MACD histogram directionSector rotation breadth20 pts% sectors above 20DMA, top vs bottom sector spreadMarket breadth (rolling A/D + 52wk H/L)20 ptsMcClellan oscillator + new highs/lows ratioVIX trend15 ptsVIX vs its 20DMA, direction of change
Score range: -100 to +100

Score > 30 → BULLISH
Score > 60 → STRONGLY_BULLISH
Score < -30 → BEARISH
Score < -60 → STRONGLY_BEARISH
Otherwise → NEUTRAL


PERFORMANCE REQUIREMENTS:

Fetch the widest needed candle window once per instrument, slice locally for each indicator
All instruments within a phase (indices, sectors, breadth) must be fetched with Task.WhenAll
All four analysis phases (AnalyzeIndicesAsync, AnalyzeSectorsAsync, CalculateMarketBreadthAsync, GetVolatilityIndexAsync) must run concurrently with Task.WhenAll at the top level
Pass a single istToday and utcNow snapshot into every helper — no DateTime.Now or DateTime.Today calls inside helpers
Single-pass min/max — no multiple .OrderBy().First() on the same list


NEW/UPDATED MODEL FIELDS NEEDED:
Add these to MarketContext and MarketSentiment if not present:
csharpdecimal RSI { get; set; }                  // market-wide average RSI
decimal MacdHistogram { get; set; }        // average MACD histogram across major indices
decimal PriceVs20DMA { get; set; }         // % above/below 20DMA (index average)
decimal PriceVs50DMA { get; set; }         // % above/below 50DMA
int NewHighs52W { get; set; }              // count of stocks at 52-week highs today
int NewLows52W { get; set; }               // count of stocks at 52-week lows today
decimal MclellanOscillator { get; set; }   // rolling breadth oscillator
decimal VixVs20DMA { get; set; }           // VIX minus its 20-day SMA

DO NOT CHANGE:

Repository interface signatures
IMarketSentimentService interface
Dependency injection constructor
GetHistoryAsync method
AdjustConfidenceForMarketSentiment method
Logging patterns already in place


OUTPUT:
Produce the complete updated MarketSentimentService.cs with all changes applied. Include private static helper methods for CalculateRSI, CalculateEMA, CalculateMACD — these should be pure functions taking List<decimal> (closing prices) and returning the indicator value.